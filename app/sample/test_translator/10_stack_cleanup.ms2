func addDouble() -> Double {
    return 5.0;
}

func add() -> Int {
    return 5;
}

func main() {
    var x = 0;
    while (x < 10 ** 8) {
        x = x + 1;
        addDouble();
    }
    x = 0;
    while (x < 10 ** 8) {
        x = x + 1;
        add();
    }
}
